# Mandlscord - Мессенджер

Полнофункциональный веб-мессенджер с поддержкой реального времени.

## Функции

- 🔐 Регистрация и авторизация
- 💬 Личные и групповые чаты
- 🎤 Голосовые сообщения
- 📷 Отправка изображений
- 😊 Эмодзи
- 🟢 Онлайн-статусы
- 🔍 Поиск пользователей по @username
- 👤 Настраиваемый профиль с аватаром

## Деплой на Render.com

### Шаг 1: Загрузите код на GitHub

1. Создайте новый репозиторий на GitHub
2. Загрузите весь код в репозиторий:

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/mandlscord.git
git push -u origin main
```

### Шаг 2: Создайте Web Service на Render

1. Зайдите на [render.com](https://render.com) и войдите
2. Нажмите **New +** → **Web Service**
3. Подключите ваш GitHub репозиторий
4. Заполните настройки:

| Настройка | Значение |
|-----------|----------|
| **Name** | mandlscord |
| **Region** | Frankfurt (EU Central) или ближайший |
| **Branch** | main |
| **Runtime** | Node |
| **Build Command** | `npm install && npm run build` |
| **Start Command** | `node server.js` |
| **Instance Type** | Free |

5. Нажмите **Create Web Service**

### Шаг 3: Дождитесь деплоя

Render автоматически соберёт и запустит приложение. 
После завершения вы получите URL вида: `https://mandlscord.onrender.com`

## Локальный запуск

### Для разработки:

```bash
# Установка зависимостей
npm install

# Запуск frontend (dev сервер)
npm run dev

# В другом терминале - запуск backend
node server.js
```

Frontend будет на `http://localhost:5173`
Backend будет на `http://localhost:3001`

### Для продакшена:

```bash
npm install
npm run build
node server.js
```

Приложение будет доступно на `http://localhost:3001`

## Технологии

- **Frontend**: React, TypeScript, Tailwind CSS, Vite
- **Backend**: Node.js, Express, Socket.io
- **Realtime**: WebSocket (Socket.io)

## Ограничения бесплатного хостинга

- На бесплатном плане Render сервис "засыпает" после 15 минут неактивности
- Первый запрос после сна может занять 30-60 секунд
- Данные хранятся в памяти и сбрасываются при перезапуске

## Для полноценного продакшена рекомендуется:

1. Добавить базу данных (PostgreSQL на Render)
2. Использовать Redis для кэширования
3. Реализовать WebRTC для голосовых/видео звонков
4. Добавить авторизацию через JWT токены
5. Хранить файлы в облачном хранилище (AWS S3, Cloudflare R2)
