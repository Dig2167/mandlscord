import React from 'react';

interface CallModalProps {
  isMobile?: boolean;
}

// Placeholder component - calls functionality would require WebRTC
export const CallModal: React.FC<CallModalProps> = ({ isMobile: _isMobile = false }) => {
  return null;
};
