# Mandlscord - Деплой на Render.com

## 🚀 Быстрый деплой

### 1. Загрузи код на GitHub
```bash
git init
git add .
git commit -m "Mandlscord messenger"
git remote add origin https://github.com/YOUR_USERNAME/mandlscord.git
git push -u origin main
```

### 2. Создай Web Service на Render.com

1. Зайди на [render.com](https://render.com)
2. Нажми **New +** → **Web Service**
3. Подключи свой GitHub репозиторий
4. Заполни настройки:

| Параметр | Значение |
|----------|----------|
| **Name** | mandlscord |
| **Environment** | Node |
| **Build Command** | `npm install && npm run build` |
| **Start Command** | `npm start` |
| **Instance Type** | Free |

5. Добавь переменную окружения (опционально):
   - **JWT_SECRET** = любая случайная строка для безопасности

6. Нажми **Create Web Service**

### 3. Готово!
После деплоя (2-5 минут) получишь URL: `https://mandlscord.onrender.com`

---

## ✨ Возможности

### Авторизация (JWT)
- ✅ Регистрация с проверкой username
- ✅ Вход по username/password
- ✅ JWT токены с автообновлением
- ✅ Защищённые API endpoints
- ✅ Хэширование паролей

### Мессенджер
- ✅ Личные чаты
- ✅ Групповые чаты
- ✅ Текстовые сообщения
- ✅ Отправка изображений
- ✅ Голосовые сообщения с прогресс-баром
- ✅ Эмодзи
- ✅ Поиск пользователей по @username
- ✅ Онлайн-статусы в реальном времени

### WebRTC Звонки
- ✅ Аудио-звонки
- ✅ Видео-звонки
- ✅ Включение/выключение камеры и микрофона
- ✅ Входящие/исходящие звонки
- ✅ Таймер разговора

### Профиль
- ✅ Загрузка аватара
- ✅ Редактирование имени и био
- ✅ Статусы: в сети, отошёл, не беспокоить, невидимый

### UI/UX
- ✅ Адаптивный дизайн (мобильные + десктоп)
- ✅ Тёмная тема
- ✅ Градиенты purple/pink/blue
- ✅ Плавные анимации

---

## 📁 Структура проекта

```
mandlscord/
├── server.js           # Express + Socket.io сервер
├── package.json        # Зависимости
├── src/
│   ├── App.tsx         # Главный компонент
│   ├── main.tsx        # Точка входа
│   ├── index.css       # Стили
│   ├── context/
│   │   └── AppContext.tsx    # Состояние приложения
│   ├── services/
│   │   └── socket.ts         # API и Socket.io клиент
│   ├── components/
│   │   ├── Auth/
│   │   │   └── AuthForms.tsx # Формы входа/регистрации
│   │   ├── Chat/
│   │   │   └── ChatWindow.tsx # Окно чата + WebRTC
│   │   ├── Sidebar/
│   │   │   └── Sidebar.tsx   # Список чатов
│   │   └── Profile/
│   │       └── Profile.tsx   # Профиль пользователя
│   └── types/
│       └── index.ts          # TypeScript типы
└── dist/               # Собранное приложение
```

---

## ⚠️ Важно

### Free Tier Render.com
- Сервер "засыпает" после 15 минут неактивности
- При пробуждении данные сохраняются в `data.json`
- Файловая система сбрасывается при новом деплое

### Для продакшена рекомендуется:
1. Добавить **PostgreSQL** базу данных
2. Использовать **Render Persistent Disk** для хранения файлов
3. Настроить **TURN-сервер** для WebRTC за NAT

---

## 🔧 Локальная разработка

```bash
# Установка зависимостей
npm install

# Запуск сервера разработки
npm run dev

# В отдельном терминале запуск бэкенда
node server.js

# Сборка
npm run build
```

Сервер: http://localhost:3001
Клиент: http://localhost:5173
